<form method="post" action="{{ path('app_ganho_delete', {'id': ganho.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ganho.id) }}">
    <button class="btn">Delete</button>
</form>
